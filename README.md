# Edge CMS

This is a CMS app designed to run on the Harper fusion platform. Pages are easy to update and render fast.

## Running with Harper CLI

```
npm install
cp .env.example .env
pico .env
npm run dev
```

(or if you enter that directory, you can run the current directory as `harperdb run .`).
